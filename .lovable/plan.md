# Cleanup after the role-hierarchy step

No publish. No user deletion. Database state stays exactly as it is.

## 1. Remove Drizzle, record the migrations properly

Delete:
- `drizzle.config.ts`
- the whole `drizzle/` directory (`schema.ts`, `migrations/0000_add_role_enum_values.sql`, `migrations/0001_role_hierarchy_functions.sql`, `migrations/meta/`)
- `drizzle-kit` and `drizzle-orm` from `package.json` dependencies

Re-record the same two migrations as plain SQL files in `supabase/migrations/`, matching the existing naming convention (`YYYYMMDDHHMMSS_<name>.sql`):
- `..._add_role_enum_values.sql` — the three `ALTER TYPE public.app_role ADD VALUE IF NOT EXISTS ...` statements
- `..._role_hierarchy_functions.sql` — the `is_admin()` rewrite, `is_developer` / `is_owner` / `is_staff` / `has_role`, their REVOKE/GRANT lines, and the `admin → developer` update

Content is copied verbatim from the two Drizzle files so the recorded history matches what actually ran. These files are records only — nothing is re-applied to the database, which is already in the correct state.

## 2. `.env` out of version control

Add `.env` (and `.env.*`, excluding `.env.example`) to `.gitignore`.

Note: the file itself must stay on disk — the preview and build read `VITE_SUPABASE_*` from it, and it is regenerated by the platform. So this step untracks it, it does not delete it. Git staging is handled by the platform; the `.gitignore` entry is the actionable part on my side.

## 3. Both developer accounts (report only)

Already queried, no changes made:

| Email | Role | Role row created | Last sign-in |
|---|---|---|---|
| rutkusmarius@gmail.com | developer | 2026-07-09 | 2026-08-25 |
| patrickahalliday@gmail.com | developer | 2026-07-15 | 2026-07-16 |

The second row (Patrick) is the likely stale/second account promoted by the `admin → developer` update. Nothing will be deleted or downgraded until you confirm.

## Verification

- (a) no `drizzle` files or deps remain (`rg drizzle` clean); the two new `supabase/migrations/*.sql` files exist with the exact SQL that ran
- (b) `.env` is gitignored and still present on disk; app still boots
- (c) both developer emails reported above
- typecheck passes; no publish
